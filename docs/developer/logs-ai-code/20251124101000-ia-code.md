# IA Change Template — Code-Brain

## 1. Contexto consumido
- docs/tasks/tasks-release.md (TREL-003..006)
- package.json, VERSION, CHANGELOG.md, scripts directory.

## 2. Objetivo da alteração
- Avançar fase de release criando automações básicas: update-version script, build:release, .npmignore e ajuste do bin para apontar para dist.

## 3. Decisões e justificativas
- Script `scripts/update-version.ts` atualiza package.json, VERSION e insere entrada no CHANGELOG; uso via `npm run update:version`.
- `build:release` reutiliza build existente (tsup) pois já produz dist limpa; evitar duplicação.
- .npmignore exclui src/tests/docs/scripts e artefatos de dev, preservando README.
- Bin agora aponta para `dist/cli/index.js`, alinhado ao build.

## 4. Implementação
- Arquivos novos: scripts/update-version.ts, .npmignore, docs/developer/logs-ai-code/20251124101000-ia-code.md.
- Arquivos alterados: package.json (bin, build:release, update:version), docs/tasks/tasks-release.md (status TREL-003..006).
- Não rodei build/publish; apenas edição de arquivos.

## 5. Revisão humana
- Verificar se entrypoint final em dist/cli/index.js corresponde ao output do tsup (format esm, no-splitting). Ajustar se bundler produzir outro path.

## 6. Follow-ups
- Implementar validação pós-build (TREL-007) e publish/dry-run (TREL-008/009).

## 7. Next Steps
- Adicionar workflows CI (TREL-010..012) e documentação final (TREL-013..015) quando prioritário.
