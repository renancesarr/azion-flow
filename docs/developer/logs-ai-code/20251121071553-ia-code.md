# IA Change Template — Code-Brain

> Salve como `code-brain/developer/logs-ai-code/<timestamp>-ia-code.md`.
> Cite sempre os IDs envolvidos (`IN-XXXX`, `UC-XXXX`, `T-XXXX`) e os validadores executados.

## 1. Contexto consumido

- context-index.yaml → AGENTS.md → project-guide.md → docs/architecture.md → docs/stack.md → docs/tasks/tasks-cli-ux.md → src/cli/index.ts → src/cli/utils/colors.ts.

## 2. Objetivo da alteração

- Executar tasks UX pendentes: TUX-003 (tabela simples) e TUX-004/TUX-005 (banner aplicado aos comandos).
- Escopo: utilitário `renderTable`, banner textual e exibição global no entrypoint.

## 3. Decisões e justificativas

- Alternativa A: esperar UX completa; descartada para avançar base.
- Alternativa B (escolhida): implementar utilitários atômicos sem dependências, mantendo integração mínima (banner no main) para atender TUX-005.
- Riscos: sem testes automatizados; mitigação futura ao integrar em comandos reais.

## 4. Implementação

- Arquivos gerados: `src/cli/utils/table.ts`, `src/cli/banner.ts`.
- Arquivo alterado: `src/cli/index.ts` (exibe banner antes do bootstrap/roteamento).
- Não executado build/test nesta iteração.

## 5. Revisão humana

- Confirmar se banner deve ser condicionado a env (`NO_COLOR`/silencioso) e se tabela precisa cabeçalho/colunas fixas.

## 6. Follow-ups

- Integrar table/section/banner nos comandos (help, deploy summary) e loaders (TUX-006/TUX-009).
- Adicionar testes de geração de strings para utils de UX.

## 7. Next Steps

- Implementar TUX-006 (loading minimalista) e TUX-008 (step labels) para orquestração.
