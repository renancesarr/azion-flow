# IA Change Template — Code-Brain

> Salve como `code-brain/developer/logs-ai-code/<timestamp>-ia-code.md`.
> Cite sempre os IDs envolvidos (`IN-XXXX`, `UC-XXXX`, `T-XXXX`) e os validadores executados.

## 1. Contexto consumido

- context-index.yaml → AGENTS.md → project-guide.md → docs/tasks/tasks-deploy-flow-real.md → src/usecases/deploy/deploy.factory.ts → tests/integration/*.

## 2. Objetivo da alteração

- Iniciar tasks TDEP-REAL-012/013/014 com testes de integração placeholder (skipped) para reservar estrutura sem quebrar build.

## 3. Decisões e justificativas

- Como steps/providers reais ainda não estão implementados, os testes foram marcados como `describe.skip` para evitar falhas. Servem de scaffolding até termos mocks reais.
- Não marcar tasks como concluídas; apenas preparação.

## 4. Implementação

- Novos arquivos:
  - `tests/integration/deploy.real.test.ts` (skip, fluxo completo mock)
  - `tests/integration/deploy.fallback.test.ts` (skip, fallback)
  - `tests/integration/deploy.error.test.ts` (skip, erro)
- Sem alterações em tasks ou código de produção.

## 5. Revisão humana

- Confirmar se abordaremos esses testes com msw/mocks dos providers ou stubs customizados.

## 6. Follow-ups

- Implementar lógica real e remover `skip` quando providers/steps reais estiverem prontos.
- Conectar testes às tasks TDEP-REAL-012/013/014 ao conclui-las.

## 7. Next Steps

- Desenvolver mocks dos providers (Azion/FS/Config) e steps reais para habilitar os testes de integração.
