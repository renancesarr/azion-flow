# IA Change Template — Code-Brain

## 1. Contexto consumido
- docs/tasks/tasks-release.md (TREL-010..012)
- package.json, scripts/validate-release.ts, GitHub workflows

## 2. Objetivo da alteração
- Criar workflows de CI/CD e atualizar scripts para suportar release/test/lint pipelines.

## 3. Decisões e justificativas
- `test.yml` roda build + tests em push/PR (Node 18).
- `lint.yml` roda markdownlint + validate-docs para manter docs consistentes.
- `release.yml` aciona em tags `v*.*.*`, roda build/validate e publica no npm com `NPM_TOKEN`, depois cria release no GitHub anexando CHANGELOG.
- NPM scripts adicionados para publish/dry-run; bin aponta para dist/index.js conforme validate-release script.

## 4. Implementação
- Arquivos novos: .github/workflows/test.yml, lint.yml, release.yml; docs/developer/logs-ai-code/20251124102500-ia-code.md.
- Arquivos alterados: package.json (scripts/bin), docs/tasks/tasks-release.md (status TREL-010..012).
- Testes de CI não executados localmente (workflows); suíte de telemetria segue verde.

## 5. Revisão humana
- Confirmar que build output final é dist/index.js; ajustar validate-release e bin se tsup gerar outro caminho.
- Garantir que CHANGELOG não fique gigante para body da release; avaliar geração parcial no futuro.

## 6. Follow-ups
- Abrir SECRET NPM_TOKEN no repositório antes do release.
- Adicionar coverage/commitlint se desejado na lint pipeline.

## 7. Next Steps
- Completar docs/examples/licença (TREL-013..018) para concluir fase de release.
