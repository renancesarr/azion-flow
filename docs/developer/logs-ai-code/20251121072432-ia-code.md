# IA Change Template — Code-Brain

> Salve como `code-brain/developer/logs-ai-code/<timestamp>-ia-code.md`.
> Cite sempre os IDs envolvidos (`IN-XXXX`, `UC-XXXX`, `T-XXXX`) e os validadores executados.

## 1. Contexto consumido

- context-index.yaml → AGENTS.md → project-guide.md → docs/architecture.md → docs/stack.md → docs/tasks/tasks-core.md.

## 2. Objetivo da alteração

- Concluir todas as tasks de core (TCORE-001 a TCORE-020), criando estrutura de contexto, erros, logger, utils e testes de smoke.

## 3. Decisões e justificativas

- Seguir fielmente placeholders solicitados, sem lógica adicional, garantindo compatibilidade de compilação.
- Adicionar pequenas normalizações em `format.ts` (trim/normalize) para evitar funções vazias inertes.
- Sem build/test executados por falta de deps instaladas; foco em scaffolding rastreável.

## 4. Implementação

- Gerados:
  - Core context: `src/core/context/index.ts`, `types.ts`, `createContext.ts`.
  - Core errors: `src/core/errors/base-error.ts`, `error-codes.ts`, `index.ts`, diretório `errors/types/` (gitkeep prévio).
  - Logger: `src/core/logger/logger.ts`, `types.ts`, `adapters/console-logger.ts`.
  - Utils: `src/core/utils/fs/path.ts`, `src/core/utils/text/format.ts`, `src/core/utils/index.ts`.
  - Tests: `tests/core/context.test.ts`, `tests/core/errors.test.ts`, `tests/core/logger.test.ts`, `tests/core/utils.test.ts`.
- Nenhum arquivo removido.

## 5. Revisão humana

- Confirmar se funções vazias/placeholder são aceitáveis ou se devemos aproximar da stack real (ex.: path wrappers usando `node:path`).
- Avaliar se `normalizeText` deve considerar locale ou lower-case; não especificado.

## 6. Follow-ups

- Executar vitest assim que dependências forem instaladas.
- Expandir utils/path com comportamento real quando tasks posteriores exigirem.

## 7. Next Steps

- Integrar core no restante do código (use cases/providers) e iniciar tasks de domínio/providers reais.
