# IA Change Template — Code-Brain

## 1. Contexto consumido
- docs/tasks/tasks-release.md (TREL-007..009)
- package.json, scripts/validate-release.ts

## 2. Objetivo da alteração
- Completar etapa de release: validação pós-build, scripts de publish e dry-run.

## 3. Decisões e justificativas
- `validate-release` verifica dist/index.js e executa `node dist/index.js --help` para garantir entrypoint funcional.
- Scripts npm: `validate:release`, `npm:publish`, `npm:publish:dry` adicionados para padronizar fluxo.
- Ajuste de bin para `dist/index.js`, alinhado ao output padrão do tsup.

## 4. Implementação
- Arquivos novos: scripts/validate-release.ts; docs/developer/logs-ai-code/20251124101600-ia-code.md.
- Arquivos alterados: package.json (bin, scripts), docs/tasks/tasks-release.md (status TREL-007..009).
- Testes não executados para release; suite de telemetria re-rodada permanece verde.

## 5. Revisão humana
- Confirmar se build gera `dist/index.js`; se tsup mudar saída, ajustar bin/validate-release.

## 6. Follow-ups
- Ainda abertos: workflows CI/release (TREL-010..012) e docs/examples/licença (TREL-013..018).

## 7. Next Steps
- Criar workflows GitHub (test, lint, release) e documentação final de produto.
