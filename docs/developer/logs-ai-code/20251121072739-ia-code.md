# IA Change Template — Code-Brain

> Salve como `code-brain/developer/logs-ai-code/<timestamp>-ia-code.md`.
> Cite sempre os IDs envolvidos (`IN-XXXX`, `UC-XXXX`, `T-XXXX`) e os validadores executados.

## 1. Contexto consumido

- context-index.yaml → AGENTS.md → project-guide.md → docs/architecture.md → docs/stack.md → docs/tasks/tasks-providers.md.

## 2. Objetivo da alteração

- Implementar todas as tasks de providers (TPROV-001 a TPROV-017): estrutura HTTP, storage, application, domain, filesystem, config, tipos globais e testes de smoke.

## 3. Decisões e justificativas

- Manter placeholders minimalistas conforme tasks (sem lógica real).
- Exportação central via `src/providers/index.ts` para facilitar imports.
- Tests de smoke alinhados aos exemplos do tasks file; dependências não executadas (sem npm install).

## 4. Implementação

- Estrutura criada:
  - HTTP: `src/providers/azion/http/{http-client.ts,types.ts,index.ts}`
  - Azion providers: `src/providers/azion/azion.storage.ts`, `azion.application.ts`, `azion.domain.ts`
  - Filesystem: `src/providers/filesystem/nodefs.ts`
  - Config: `src/providers/config/file-config.ts`
  - Tipos/exports: `src/providers/types.ts`, `src/providers/index.ts`
- Testes de smoke:
  - `tests/providers/http.test.ts`
  - `tests/providers/storage.test.ts`
  - `tests/providers/application.test.ts`
  - `tests/providers/domain.test.ts`
  - `tests/providers/filesystem.test.ts`
  - `tests/providers/config.test.ts`
- Nenhum arquivo removido.

## 5. Revisão humana

- Confirmar se usamos `cwd` ou diretório específico para providers de config/FS futuramente.
- Verificar se futuros ports precisam de interfaces adicionais além de `Provider {}`.

## 6. Follow-ups

- Rodar vitest quando dependências estiverem instaladas.
- Evoluir providers com lógica real nas tasks subsequentes (ex.: TAZ-xxx, TSERVREAL, TSTEPREAL).

## 7. Next Steps

- Iniciar tasks de domínio/services reais ou steps reais, conforme priorização de deploy flow.
