{
  "name": "azion-flow",
  "version": "0.0.1",
  "type": "module",
  "description": "CLI modular para deploy de sites estáticos na Azion.",
  "license": "MIT",
  "bin": {
    "azion-flow": "./bin/azion-flow"
  },
  "scripts": {
    "build": "tsup src/cli/index.ts --format esm --dts --minify --no-splitting --clean",
    "test": "vitest",
    "lint:md": "markdownlint \"**/*.md\" --ignore node_modules --ignore dist --ignore docs/developer --ignore batch-executor.md",
    "validate-docs": "node scripts/validate-docs.js"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.39.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
